'use strict';

import React from 'react';

require('styles/lists/AudioListItem.sass');

let AudioListItemComponent = (props) => (
  <div className="audiolistitem-component">
    Please edit src/components/lists//AudioListItemComponent.js to update this component!
  </div>
);

AudioListItemComponent.displayName = 'ListsAudioListItemComponent';

// Uncomment properties you need
// AudioListItemComponent.propTypes = {};
// AudioListItemComponent.defaultProps = {};

export default AudioListItemComponent;
